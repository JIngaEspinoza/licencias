import { PrismaClient } from "@prisma/client";
const prisma = new PrismaClient();

export async function seedDjs() {
  // Caso 1: Restaurante
  // =========================================
  // Caso 2: Clínica
  // =========================================
  // =========================================
  // Caso 3: Patrimonio (Museo/Galería)
  // =========================================
}